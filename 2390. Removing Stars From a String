class Solution {
public:
    string removeStars(string s) {
        string l;
        for(char i:s){
            if(i=='*')
                l.pop_back();
            else
                l.push_back(i);
        }
        return l;
    }
};
